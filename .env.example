# FitCSV Environment Configuration
# Copy this file to .env and configure for your setup

# ⚠️ IMPORTANT: If you have .env.local or other .env.* files, they will
# override values in this file! Update ALL .env files or delete the extras.
# File precedence: .env < .env.local < .env.development < .env.development.local

# =============================================================================
# Database - Choose ONE option:
# =============================================================================

# Option A: Supabase (cloud, includes auth)
# Get connection strings from: https://supabase.com/dashboard/project/_/settings/database
# DATABASE_URL="postgresql://postgres.xxxx:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres"
# DIRECT_URL="postgresql://postgres:[password]@db.xxxx.supabase.co:5432/postgres"

# Option B: Local PostgreSQL (Docker)
# Run: docker run -d --name fitcsv-postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=fitcsv -p 5433:5432 postgres:15
# Note: Using port 5433 to avoid conflicts with existing PostgreSQL installations
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/fitcsv"
DIRECT_URL="postgresql://postgres:postgres@localhost:5433/fitcsv"

# Option C: Testing with testcontainers (leave empty, testcontainers will set these)
# DATABASE_URL=""
# DIRECT_URL=""

# =============================================================================
# Supabase API Credentials (Required for authentication)
# =============================================================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL="https://xxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# =============================================================================
# Application Settings
# =============================================================================
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# =============================================================================
# PowerSync (Optional - only if using PowerSync integration)
# =============================================================================
# NEXT_PUBLIC_POWERSYNC_URL="https://xxxx.powersync.journeyapps.com"
# POWERSYNC_2_SUPABASE="your-powersync-key"
